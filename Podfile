source 'https://github.com/CocoaPods/Specs.git'
platform :ios, '8.0'
use_frameworks!

target 'PrettyCircleView' do
	pod 'PrettyExtensionsKit'

	target 'Example' do
    	inherit! :search_paths
  	end
end
